import styled from 'styled-components';

const BoxNumber = styled.span`
  color: #1654f0;
  font-size: 3rem;
  text-align: center;
  user-select: none;
  font-weight: 500;
`;

export default BoxNumber;
